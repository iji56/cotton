import { StyleSheet } from 'react-native';

const AddPhotoButton = StyleSheet.create({
  container: {
    backgroundColor: '#F2F2F2',
    height: 160,
    borderRadius: 8,
    alignItems: 'center',
    justifyContent: 'center',
  },
  text: { marginTop: 8 },
});

export default AddPhotoButton;
