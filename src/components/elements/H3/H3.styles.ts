import { fontScale } from '@/utils/fontScale';
import { StyleSheet } from 'react-native';

const SH3 = StyleSheet.create({
  text: {
    fontSize: fontScale(12),
    fontWeight: '700',
  },
});

export default SH3;
