import { fontScale } from '@/utils/fontScale';
import { StyleSheet } from 'react-native';

const SH1 = StyleSheet.create({
  text: {
    fontSize: fontScale(24),
    fontWeight: '700',
  },
});

export default SH1;
