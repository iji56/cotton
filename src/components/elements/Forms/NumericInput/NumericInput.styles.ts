import { StyleSheet } from 'react-native';

const NumericInput = StyleSheet.create({
  input: {
    height: 56,
    borderColor: '#79747E',
    borderWidth: 1,
    marginBottom: 4,
    paddingLeft: 16,
    borderRadius: 4,
  },
});

export default NumericInput;
