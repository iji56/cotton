import { StyleSheet } from "react-native";

export const SPS = StyleSheet.create({
  mainContainer: {
    paddingVertical: 10,
  },
  container: {
    display: 'flex',
    flexDirection: 'row',
    justifyContent: 'space-between',
    marginBottom: 12
  },
  totalValue: {
    fontWeight: 'bold'
  }
});