import { StyleSheet } from 'react-native';

export const SFF = StyleSheet.create({
  container: {
    paddingVertical: 8,
    marginRight: 1.5,
  },
  filterList: {
    display: 'flex',
    flexDirection: 'row',
    gap: 8,
    marginBottom: 8,
  },
});
