import { StyleSheet } from 'react-native';

const SS = StyleSheet.create({
  container: {
    marginBottom: 5,
    marginTop: 5,
    flex: 1,
    flexDirection: 'row',
    flexWrap: 'wrap',
    justifyContent: 'space-between',
  },
});

export default SS;