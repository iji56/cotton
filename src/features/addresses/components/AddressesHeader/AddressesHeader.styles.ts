import { StyleSheet } from "react-native";

const AHS = StyleSheet.create({
    container: {
        flexDirection: 'row',
        marginTop: 20,
        alignItems: 'center',
        marginLeft: 20
    },
    icon: {
        marginRight: 15
    }
});

export default AHS;