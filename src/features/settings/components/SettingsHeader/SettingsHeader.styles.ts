import { StyleSheet } from 'react-native';

const SSH = StyleSheet.create({
  container: {
    paddingBottom: 10,
  },
  mainContainer: {
    display: 'flex',
    flexDirection: 'row',
    justifyContent: 'flex-end',
  },
  containerNavItem: {
    marginLeft: 12,
  },
  settingsContainer: {
    display: 'flex',
    flexDirection: 'row',
    marginTop: 20,
    marginBottom: 10,
  },
  headerTitle: {
    fontSize: 16,
  },
  backButton: {
    marginRight: 20,
    alignSelf: 'center'
  }
});

export default SSH;